enum AnalyticsKey: String {
    case appLaunched = "app_launched"
    case blockedAppAttempt = "blocked_app_attempt"
    case timerAborted = "timer_aborted"
    case timerCompleted = "timer_completed"
    case timerPaused = "timer_paused"
    case timerResumed = "timer_resumed"
    case timerSkipped = "timer_skipped"
    case timerStarted = "timer_started"
}
